{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "svc-test",
    "annotations": {
      "test": "original"
    }
  },
  "spec": {
    "selector": {
      "test": "selector"
    },
    "ports": [
      {
        "protocol": "TCP",
        "port": 80,
        "targetPort": 8080
      }
    ]
  }
}
